[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scenerx-backend"
version = "1.0.0"
description = "SceneRx API - Urban greenspace visual analysis backend"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "SceneRx Team"}
]
dependencies = [
    # FastAPI and server
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "python-multipart>=0.0.6",
    # Pydantic
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "email-validator",
    # HTTP client
    "httpx>=0.26.0",
    # Environment
    "python-dotenv>=1.0.0",
    # Data processing
    "pandas>=2.1.0",
    "numpy>=1.26.0",
    "openpyxl>=3.1.0",
    "scipy>=1.11.0",
    "scikit-learn>=1.3.0",
    # Image processing
    "opencv-python-headless>=4.9.0",
    "pillow>=10.2.0",
    # AI/ML — LLM providers
    "google-genai>=1.0.0",
    "openai>=1.0.0",
    "anthropic>=0.30.0",
    # Task Queue
    "celery[redis]>=5.3.0",
    "redis>=5.0.0",
    # Authentication
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.0",
    # Database
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.29.0",
    "psycopg2-binary>=2.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.26.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
